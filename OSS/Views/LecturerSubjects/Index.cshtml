@model IEnumerable<OSS.Models.SurveySystemModels.LecturerSubject>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lecturer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lecturer.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject.FullName)
            </td>
            <td>
                <a asp-controller="LecturerSubjects" asp-action="Edit" asp-route-Lid="@item.LecturerId" asp-route-Sid="@item.SubjectId">Edit</a> |
                @*@Html.ActionLink("Edit", "LecturerSubjects", new { Lid = item.LecturerId, Sid = item.SubjectId }) |*@
                @Html.ActionLink("Details", "LecturerSubjects", new { Lid = item.LecturerId, Sid = item.SubjectId }) |
                @Html.ActionLink("Delete", "LecturerSubjects", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
